{
  "name": "distributed-textbook-compiler",
  "version": "1.0.0",
  "private": true,
  "description": "A collaborative learning system for creating modular academic content",
  "scripts": {
    "dev": "pnpm --parallel --stream dev",
    "build": "pnpm --recursive --stream build",
    "test": "pnpm --recursive --stream test",
    "lint": "pnpm --recursive --stream lint",
    "clean": "pnpm --recursive --stream clean"
  },
  "keywords": [
    "education",
    "textbook",
    "compiler",
    "markdown",
    "collaborative"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}

